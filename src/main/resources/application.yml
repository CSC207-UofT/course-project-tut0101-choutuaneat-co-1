spring:
    jpa:
        database-platform: org.hibernate.dialect.H2Dialect
        hibernate.ddl-auto: update
        generate-ddl: true
    h2:
        console.enabled: true
        console.path: /h2
    datasource:
        url: jdbc:h2:file:D:/sample #jdbc:h2:file:/data/sample (Windows only)
        username: choutuan
        password: password
        driver-class-name: org.h2.Driver
logging:
    level:
        root: debug